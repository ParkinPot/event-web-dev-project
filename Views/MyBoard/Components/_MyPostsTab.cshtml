<!-- _MyPostsTab.cshtml -->

@model PostCardModel

<div class="stats-grid stats-grid--5">
    <div class="stat-card">
        <div class="stat-label">Total Posts</div>
        <div class="stat-value stat-value--default">2</div>
    </div>
    <div class="stat-card">
        <div class="stat-label">Open</div>
        <div class="stat-value stat-value--green">1</div>
    </div>
    <div class="stat-card">
        <div class="stat-label">Closed</div>
        <div class="stat-value stat-value--red">1</div>
    </div>
    <div class="stat-card">
        <div class="stat-label">Expired</div>
        <div class="stat-value stat-value--default">0</div>
    </div>
    <div class="stat-card">
        <div class="stat-label">Applications</div>
        <div class="stat-value stat-value--blue">7</div>
    </div>
</div>

<div class="posts-grid">
    <div class="post-card">
        <div class="post-card__header">
            <div class="post-card__tags">
                <span class="badge badge--category">@Model.Category</span>
                <span class="badge badge--open">@Model.Status</span>
            </div>
            <span class="post-card__time">@Model.PublishDate</span> <!-- unfinished -->
        </div>

        <h3 class="post-card__title">@Model.Title</h3>
        <p class="post-card__desc">@Model.Description</p>

        <div class="post-card__meta">
            <div class="meta-item">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
                <span>@Model.Location</span>
            </div>
            <div class="meta-item">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
                <span>@Model.CurrentMember / @Model.MaxMember</span>
                <span class="spots-left">(<span class="spots-highlight">spots left</span>)</span>
            </div>
            <div class="meta-item">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
                <span>Expires: @Model.ExpirationDate</span>
            </div>
        </div>

        <div class="post-card__footer">
            <span class="post-author">by @Model.Author</span>
            <span class="post-apps">@Model.NumApplication applications</span>
        </div>
    </div>
</div>
